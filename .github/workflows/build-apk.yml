name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout代码
      uses: actions/checkout@v4
    
    - name: 🐍 设置Python环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    
    - name: ☕ 安装Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '11'
    
    - name: 📦 安装系统依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          git \
          zip \
          unzip \
          openjdk-11-jdk \
          autoconf \
          automake \
          libtool \
          libtool-bin \
          pkg-config \
          zlib1g-dev \
          libncurses5-dev \
          libffi-dev \
          libssl-dev \
          python3-dev \
          cmake \
          gettext \
          expect
    
    - name: 📦 安装Python依赖
      run: |
        pip install --upgrade pip
        pip install buildozer cython
    
    - name: 🔧 配置构建环境
      run: |
        echo "BUILDOZER_WARN_ON_ROOT=0" >> $GITHUB_ENV
        echo "ANDROID_SDK_ROOT=$HOME/.buildozer/android/platform/android-sdk" >> $GITHUB_ENV
        echo "ANDROID_HOME=$HOME/.buildozer/android/platform/android-sdk" >> $GITHUB_ENV
        # 强制使用系统libffi，不重新编译
        echo "LIBFFI_INCLUDEDIR=/usr/include" >> $GITHUB_ENV
        echo "LIBFFI_LIBDIR=/usr/lib/x86_64-linux-gnu" >> $GITHUB_ENV
        echo "PKG_CONFIG_PATH=/usr/lib/x86_64-linux-gnu/pkgconfig:/usr/lib/pkgconfig:/usr/share/pkgconfig" >> $GITHUB_ENV
    
    - name: 🔨 构建APK
      run: |
        # 设置为非交互模式，自动接受许可证
        export CI=true
        export LIBFFI_INCLUDEDIR=/usr/include
        export LIBFFI_LIBDIR=/usr/lib/x86_64-linux-gnu
        export PKG_CONFIG_PATH=/usr/lib/x86_64-linux-gnu/pkgconfig:/usr/lib/pkgconfig:/usr/share/pkgconfig
        buildozer -v android debug
      env:
        BUILDOZER_WARN_ON_ROOT: 0
        CI: true
        LIBFFI_INCLUDEDIR: /usr/include
        LIBFFI_LIBDIR: /usr/lib/x86_64-linux-gnu
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig:/usr/lib/pkgconfig:/usr/share/pkgconfig
    
    - name: 📤 上传APK
      uses: actions/upload-artifact@v4
      with:
        name: graborder-apk
        path: bin/*.apk
        if-no-files-found: error
    
    - name: 📊 显示APK信息
      run: |
        echo "✅ APK构建成功！"
        ls -lh bin/*.apk

