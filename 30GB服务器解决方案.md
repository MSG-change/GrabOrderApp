# 🎯 30GB服务器构建Kivy APK - 解决方案

## 问题
- Android构建需要 15-20GB 空间
- 您的服务器只有 30GB

## ✅ 可行方案

---

## 方案1：GitHub Actions 自动构建（推荐）⭐⭐⭐⭐⭐

### 优势
- ✅ **完全免费**
- ✅ **空间充足**（虚拟机有足够空间）
- ✅ **自动化**
- ✅ **无需本地资源**

### 步骤

#### 1. 创建GitHub仓库（私有）
```bash
cd /Users/duanzubin/develop/script/siam-autolabel/GrabOrderApp

# 初始化git
git init
git add .
git commit -m "Initial commit"

# 创建GitHub私有仓库，然后推送
git remote add origin https://github.com/你的用户名/GrabOrderApp.git
git push -u origin main
```

#### 2. 创建GitHub Actions配置

创建 `.github/workflows/build.yml`：

```yaml
name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y openjdk-11-jdk zip unzip
        pip install buildozer cython
    
    - name: Build APK
      run: |
        buildozer android debug
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: android-apk
        path: bin/*.apk
```

#### 3. 推送并自动构建
```bash
git add .github/workflows/build.yml
git commit -m "Add build workflow"
git push

# GitHub会自动构建，30-60分钟后下载APK
```

---

## 方案2：本地虚拟机Linux（推荐）⭐⭐⭐⭐⭐

### 使用UTM（Mac上的虚拟机）

#### 优势
- ✅ 本地控制
- ✅ 无网络限制
- ✅ 可重复使用

#### 步骤

1. **安装UTM**
```bash
brew install --cask utm
```

2. **下载Ubuntu镜像**
- Ubuntu 22.04 Server（约1GB）
- 给虚拟机分配 30GB 磁盘

3. **启动虚拟机并构建**
```bash
# 在虚拟机内
cd ~
# 传输项目文件（使用scp或共享文件夹）
tar -xzf GrabOrderApp.tar.gz
cd GrabOrderApp

# 安装依赖
sudo apt update
sudo apt install -y python3-pip openjdk-11-jdk
pip3 install buildozer cython

# 构建
buildozer android debug

# 构建完成后，从虚拟机复制APK到Mac
```

---

## 方案3：云服务器临时扩容（按需付费）⭐⭐⭐⭐

### 阿里云/腾讯云按量计费

#### 步骤

1. **创建临时服务器**
   - 配置：2核8G，系统盘 50GB
   - 按量计费（约 0.5元/小时）

2. **上传并构建**
```bash
# 传输文件
scp GrabOrderApp.tar.gz root@临时服务器:/root/

# SSH登录
ssh root@临时服务器

# 构建
cd /root
tar -xzf GrabOrderApp.tar.gz
cd GrabOrderApp
apt update
apt install -y python3-pip openjdk-11-jdk
pip3 install buildozer cython
buildozer android debug

# 下载APK
# 在本地执行
scp root@临时服务器:/root/GrabOrderApp/bin/*.apk ./
```

3. **删除服务器**
   - 构建完成后立即删除
   - 总成本：1-2小时 × 0.5元 = **约1元**

---

## 方案4：使用在线构建服务 ⭐⭐⭐

### Replit（免费）

1. 访问 https://replit.com
2. 创建Python项目
3. 上传代码
4. 运行构建命令

---

## 方案5：清理现有服务器构建 ⭐⭐

### 如果坚持用现有30GB服务器

#### 严格控制空间

```bash
# 1. 清理现有空间
apt clean
apt autoremove
docker system prune -a  # 如果有Docker
rm -rf /tmp/*
rm -rf /var/log/*  # 谨慎操作

# 2. 只下载必需组件
cd /root
tar -xzf GrabOrderApp.tar.gz
cd GrabOrderApp

# 3. 修改buildozer.spec，减少编译架构
# 只编译 arm64-v8a（不编译 armeabi-v7a）
# android.archs = arm64-v8a

# 4. 构建
buildozer android debug

# 5. 构建完成后立即清理
rm -rf .buildozer
```

---

## 📊 方案对比

| 方案 | 成本 | 难度 | 推荐度 | 时间 |
|------|------|------|--------|------|
| **GitHub Actions** | 免费 | ⭐⭐ | ⭐⭐⭐⭐⭐ | 自动 |
| **本地虚拟机** | 免费 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 60分钟 |
| **临时云服务器** | ~1元 | ⭐⭐ | ⭐⭐⭐⭐ | 60分钟 |
| **在线构建** | 免费 | ⭐⭐ | ⭐⭐⭐ | 自动 |
| **清理现有服务器** | 免费 | ⭐⭐⭐⭐ | ⭐⭐ | 60分钟 |

---

## 🎯 我的推荐

### **方案1：GitHub Actions** 或 **方案2：本地虚拟机**

理由：
1. **GitHub Actions**：一键自动化，完全免费
2. **本地虚拟机**：完全掌控，可重复使用

---

## 🚀 下一步

您选择哪个方案？

1. **GitHub Actions** - 我帮您配置
2. **本地虚拟机** - 我提供详细步骤
3. **临时云服务器** - 我给您完整命令
4. **清理现有服务器** - 我给您清理脚本

**回复数字！**

