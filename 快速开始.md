# ⚡ 快速开始 - 抢单助手

## 🎯 目标

5 分钟内完成 APK 构建和安装！

---

## 📋 前置条件

```bash
# 1. 检查 Python 版本（需要 3.7+）
python3 --version

# 2. 检查 pip
pip3 --version
```

---

## 🚀 三步快速部署

### 步骤 1: 安装 Buildozer

```bash
# macOS
pip3 install buildozer cython

# Ubuntu/Debian
sudo apt install -y python3-pip git zip unzip openjdk-11-jdk
pip3 install buildozer cython
```

### 步骤 2: 构建 APK

```bash
cd GrabOrderApp
./build_apk.sh
```

**等待时间**: 首次 30-60 分钟（后续 5-10 分钟）

### 步骤 3: 安装到手机

```bash
# 方式 A: USB 安装
adb install bin/*.apk

# 方式 B: 直接传输
# 将 bin/*.apk 传到手机，点击安装
```

---

## 📱 首次使用

### 1. 打开应用

<img src="docs/screenshot_1.png" width="300">

### 2. 授予权限

点击 "允许" 授予以下权限：

- ✅ VPN 权限（核心功能）
- ✅ 网络权限
- ✅ 存储权限

### 3. 启用 VPN 抓包

- 确保 "自动获取Token" 开关为 **ON**

### 4. 登录目标应用

- 打开抢单应用（如 "顺辉智送"）
- 正常登录
- **返回抢单助手，Token 自动出现！**

### 5. 开始抢单

- 点击 **"启动抢单"** 按钮
- 查看日志确认运行

---

## 🔥 一键命令（适合高级用户）

```bash
# 完整流程
cd /Users/duanzubin/develop/script/siam-autolabel/GrabOrderApp && \
./build_apk.sh && \
adb install -r bin/*.apk && \
echo "✅ 安装完成！现在打开手机上的【抢单助手】应用"
```

---

## 💡 常用命令

```bash
# 查看连接的设备
adb devices

# 卸载旧版本
adb uninstall com.graborder

# 重新安装
adb install -r bin/*.apk

# 查看应用日志
adb logcat -s python
```

---

## ⚠️ 遇到问题？

### 问题 1: buildozer 未找到

```bash
# 解决
pip3 install --upgrade buildozer cython
```

### 问题 2: 构建失败

```bash
# 清理缓存重试
buildozer android clean
./build_apk.sh
```

### 问题 3: 手机无法安装

- 检查是否启用 "允许未知来源"
- 卸载旧版本后重试

---

## 📞 下一步

阅读完整文档：`README.md`

---

**🎉 开始抢单！**

